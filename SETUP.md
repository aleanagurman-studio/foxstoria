# FoxStoria — запуск

Сайт в `web/` можно открыть как HTML. Чтобы каталог и новые работы жили в базе, нужен бэкенд.

## Сайт без API

Откройте `web/index.html` или GitHub Pages. Работы хранятся в браузере.

## Бэкенд (Python 3.12, не 3.14)

```bash
cd backend
./install.sh
./run.sh
```

Дальше только **http://127.0.0.1:8000/** — и страницы, и `/api`.

Проверка: http://127.0.0.1:8000/api/health
