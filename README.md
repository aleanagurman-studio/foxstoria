# FoxStoria

Платформа интерактивных и линейных историй.

Сейчас это **статический сайт** в папке `web/`. Его можно выложить на хостинг **без бэкенда** — GitHub Pages, Netlify, Cloudflare Pages.

Позже, когда появятся аккаунты, оплаты и редактор с сохранением, бэкенд подключается отдельно. Макеты страниц от этого не ломаются.

## Кабинет автора (архитектура)

У каждой работы есть **публичная карточка** (то, что видит читатель) и **кабинет** (только автор).

На карточке любой работы, линейной или интерактивной:

- название, автор, редактор, соавтор
- возрастной рейтинг: без рейтинга, 16+ (лёгкие намёки), 18+
- фандом (обязательно; свои миры — «Ориджинал»)
- направленность: джен, гет, слэш, фемслэш
- описание и примечание автора
- статус: в процессе или завершена
- размер только после завершения: мини до 20 глав, миди до 50, макси дальше

Кабинет:

- **Редактор** — главы, сцены, публикация. Это текст, который потом выходит на сайт.
- **Хронология** — время мира, не порядок чтения. Линейная история: одна шкала. Интерактивная: дерево/граф веток.
- **Персонажи** — приватная библия: имя, черты, заметки. Читатель этого не видит.
- **Заметки** — сюжет, идеи, диалоги, будущие события.
- **Статистика и настройки** — тип истории, тариф ИИ.

ИИ (тариф Plus, позже): черновик сводки главы и предложения по персонажам из текста. Автор правит сам. Обычный тариф пишет сводки вручную. ИИ **не публикует** текст и **не перезаписывает** поля с замком.

Макет кабинета истории: `web/studio.html`. Модели: `backend/app/models/studio.py`.

## Аккаунт (меню)

Один человек — и читатель, и автор. Аватар в шапке открывает меню:

- **Профиль** — публичная страница: аватар, ник, подписчики, био, ссылки, работы, соавторство/редактура, открытые сборники, статистика, лента постов.
- **Сообщения** — личка.
- **Кабинет автора** — список работ, отзывы, вход в редактор / хронологию / заметки.
- **Кабинет читателя** — подписки на авторов и работы, лайки, история, свои сборники.
- **Настройки** — пароль и вход, оповещения, чёрный список, редактор.

Макеты: `web/profile.html`, `library.html`, `author-home.html`, `messages.html`, `settings.html`. Модели: `backend/app/models/account.py`.

## Что уже есть

| Страница | Файл |
|---|---|
| Главная | `web/index.html` |
| Поиск с фильтрами | `web/search.html` |
| Авторы | `web/authors.html` |
| Интерактивные работы | `web/stories-interactive.html` |
| Линейные работы | `web/stories-linear.html` |
| Редактор (макет) | `web/editor.html` |
| Профиль | `web/profile.html` |
| Кабинет автора (список) | `web/author-home.html` |
| Кабинет истории | `web/studio.html` |
| Кабинет читателя | `web/library.html` |
| Сообщения | `web/messages.html` |
| Настройки | `web/settings.html` |

Данные на карточках пока макетные.

## Как открыть без сервера

Откройте файл `web/index.html` в браузере. Python и npm не нужны.

## GitHub Pages

После push в `main` сайт собирается из папки `web/`.

1. GitHub → Settings → Pages → Source: **GitHub Actions**
2. Через 1–2 минуты сайт будет по адресу:
   `https://<логин>.github.io/foxstoria/`

## Бэкенд (Python 3.12)

Системный Python 3.14 не ставит FastAPI. Нужен **3.12**.

```bash
cd backend
./install.sh    # один раз, берёт python3.12
./run.sh        # сайт + API
```

Откройте **http://127.0.0.1:8000/** — оттуда же отдаётся `web/`, каталог и кабинеты ходят в `/api`.

- Карточки работ: SQLite `backend/foxstoria.db`
- Черновики не попадают в каталог, пока не нажать публикацию
- Текст редактора пишется в работу (`/api/works/{id}/content`)
- Документация API: http://127.0.0.1:8000/docs

Если база уже заполнялась старым seed со демо-историями — удалите `backend/foxstoria.db` и снова `./run.sh`.

Без бэкенда сайт по-прежнему открывается как статика; тогда работы остаются в `localStorage` этого браузера.
